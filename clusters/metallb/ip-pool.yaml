apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: default-address-pool
  namespace: metallb-system
spec:
  addresses:
    - 192.168.40.200-192.168.40.254 # Example IP range, adjust to your network's range
  autoAssign: true
  avoidBuggyIPs: true
